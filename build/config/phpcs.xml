<?xml version="1.0"?>
<ruleset name="PHP">
    <description>The coding standard for PHP application</description>
    <exclude-pattern>*.yml</exclude-pattern>
    <exclude-pattern>*.xml</exclude-pattern>

    <!-- Include the whole PSR-2 standard -->
    <rule ref="PSR2"/>
</ruleset>
